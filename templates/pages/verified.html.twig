{% extends 'base.html.twig' %}

{% block title %}Compte vérifié{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow-sm border-0 text-center">
                    <div class="card-header bg-success text-white py-3 border-0">
                        <h5 class="mb-0 fw-light text-uppercase ls-1">Confirmation</h5>
                    </div>
                    <div class="card-body p-5">
                        <div class="mb-4 text-success">
                            <i class="icofont-check-circled icofont-5x"></i>
                        </div>
                        <h1 class="h3 card-title mb-3">Félicitations !</h1>
                        <p class="lead text-muted mb-4">
                            Votre adresse email a été vérifiée avec succès.
                        </p>
                        <p class="card-text mb-5">
                            Vous faites désormais partie de la communauté. Vous pouvez maintenant commenter les œuvres, ajouter des favoris et participer à la vie de la galerie.
                        </p>
                        <div class="d-grid gap-2 col-sm-8 mx-auto">
                                <button type="button" class="btn btn-primary btn-lg" data-bs-toggle="modal" data-bs-target="#loginModal">
                                    <i class="icofont-login me-2"></i> Me connecter
                                </button>
                                <a href="{{ path('app_gallery') }}" class="btn btn-link text-decoration-none text-muted mt-2">
                                    Retourner à l'accueil
                                </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
