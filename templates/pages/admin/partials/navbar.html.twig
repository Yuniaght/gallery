<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand logo" href="{% if is_granted('ROLE_ADMIN') %}{{ path('app_admin_panel')}} {% endif %}">Gallery - Administration</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mx-auto">
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_admin_panel')}}">Panneau Administration</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_artists_panel') }}">Artistes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">Oeuvres</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">Catégorie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">Techniques</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="">Utilisateurs</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="">Commentaires</a>
                </li>
            </ul>
            <div class="d-flex">
                    <div class="dropdown">
                        <button class="btn btn-sm btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{ asset('images/user/' ~ app.user.image) }}" alt="{{ app.user.image }}" class="img-menu"> {{ app.user.userName }}
                        </button>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('app_user_profile') }}" class="dropdown-item">Mon profil</a></li>
                            <li><a href="{{ path('app_favorites') }}" class="dropdown-item">Mes favoris</a></li>
                            <li><a href="{{ path('app_change_info') }}" class="dropdown-item">Modifier le profil</a></li>
                            <li class="dropdown-divider"></li>
                            <li><a href="{{ path('app_logout') }}" class="dropdown-item">Se déconnecter</a></li>
                            <li class="dropdown-divider"></li>
                            <li><a href="{{ path('app_home') }}" class="dropdown-item">Retour au site</a></li>
                        </ul>
                    </div>
            </div>
        </div>
    </div>
</nav>
